services:
  claude-code:
    image: hackebrot/claude-code:${CLAUDE_CODE_VERSION:-latest}-go${GO_VERSION:-1.25.4}-py${PYTHON_VERSION:-3.14}
    build:
      target: claude-all
      args:
        GO_VERSION: ${GO_VERSION:-1.25.4}
        PYTHON_VERSION: ${PYTHON_VERSION:-3.14}
